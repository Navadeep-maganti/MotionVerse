[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "motionverse"
version = "0.1.0"
description = "Real-time vision-based motion control and gesture gaming interface"
authors = [{ name = "MotionVerse Team" }]
readme = "README.md"
requires-python = ">=3.9"
